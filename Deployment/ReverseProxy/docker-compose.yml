version: "3.9"
services:
  nginx:
    container_name: nginx-rp
    restart: always
    build:
      context: .
      dockerfile: nginx-rp.dockerfile
    volumes:
      - ../certs:/certs
    ports:
      - "6000"