version: "3.9"
services:
  nginx:
    container_name: nginx-rp
    restart: always
    build:
      context: .
      dockerfile: nginx-rp.dockerfile
    volumes:
      - /var/snap/docker/common/var-lib-docker/volumes/ReverseProxy/_data/reverse_proxy/certs:/certs
    ports:
      - "443"