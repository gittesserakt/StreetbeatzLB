version: "3.9"
services:
    builder:
        container_name: builder-sb
        restart: "no"
        build:
            context: .
            dockerfile: builder-sb/builder.dockerfile
        env_file:
            - ${PROJECT_PATH}/Environment/.env
        volumes:
            - ${PROJECT_PATH}:/data